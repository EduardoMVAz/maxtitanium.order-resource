server:
  port: 0

spring:
  application:
    name: max-order
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/max}
    username: ${DATABASE_USERNAME:max}
    password: ${DATABASE_PASSWORD:max}
    driver-class-name: org.postgresql.Driver
  flyway:
    baseline-on-migrate: true
    schemas: order
  jpa:
    properties:
      hibernate:
        default_schema: order
  redis:
    host: max-redis
    port: 6379

management:
  endpoints:
    web:
      base-path: /orders/actuator
      exposure:
        include: ['*', 'prometheus']

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}

springdoc:
  api-docs:
    path: /orders/v3/api-docs
  swagger-ui:
    path: /orders/swagger-ui