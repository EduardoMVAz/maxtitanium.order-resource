server:
  port: 0

spring:
  application:
    name: max-order
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/max}
    username: ${POSTGRES_USER:max}
    password: ${POSTGRES_PASSWORD:max}
    driver-class-name: org.postgresql.Driver
  flyway:
    baseline-on-migrate: true
    schemas: t_order
  jpa:
    properties:
      hibernate:
        default_schema: t_order
  redis:
    host: redis
    port: 6379

management:
  endpoints:
    web:
      base-path: /orders/actuator
      exposure:
        include: ['*', 'prometheus']

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://${DISCOVERY_HOST:max-discovery}:8761/eureka/

springdoc:
  api-docs:
    path: /orders/v3/api-docs
  swagger-ui:
    path: /orders/swagger-ui